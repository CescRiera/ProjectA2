class Test {
    constructor(id, name, series, timeLimit) {
      this.id = id;
      this.name = name;
      this.series = series; // Array of Series objects
      this.timeLimit = timeLimit; // Time limit in seconds
    }
  }
  